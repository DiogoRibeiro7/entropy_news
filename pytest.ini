[pytest]
markers =
    integration: marks tests that execute end-to-end workflows (deselect with '-m "not integration"').
    performance: marks performance related checks (deselect with '-m "not performance"').
    stress: marks long-running reliability scenarios (deselect with '-m "not stress"').
